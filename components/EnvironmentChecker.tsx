
import React from 'react';

/**
 * EnvironmentChecker is disabled because guidelines strictly prohibit generating UI elements
 * that ask the user for an API key or provide configuration instructions for it.
 */
const EnvironmentChecker: React.FC = () => {
  return null;
};

export default EnvironmentChecker;
